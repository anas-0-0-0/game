# 🔧 حل مشكلة Firebase - قواعد الأمان

## المشكلة:
القواعد الحالية تمنع القراءة والكتابة:
```json
".read": false,
".write": false
```

## الحل السريع:

### 1. في Firebase Console
- ادخل إلى: **https://console.firebase.google.com**
- اختر مشروعك: **myweb-1ae58**
- من القائمة الجانبية: **Build** → **Realtime Database**
- إذا كانت معطلة، اضغط **إنشاء قاعدة البيانات** (Create Database)

### 2. اختيار موقع قاعدة البيانات
- اختر موقعاً قريباً (مثل `europe-west1` أو `us-central1`)
- اضغط **التالي**

### 2. تعديل قواعد الأمان (Rules)
- اضغط تبويب **Rules**
- **احذف** القواعد الحالية واستبدلها بهذا بالكامل:

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

- اضغط **نشر** (Publish)
- انتظر حتى تظهر رسالة أن القواعد تم نشرها

### 4. التحقق من الرابط
- بعد التفعيل، تأكد من أن **databaseURL** في `firebase-config.js` صحيح:
```
https://myweb-1ae58-default-rtdb.firebaseio.com
```

### 5. إعادة المحاولة
- بعد التفعيل، جرب الانضمام مرة أخرى
- يجب أن تختفي رسالة الخطأ

---

## ملاحظة:
إذا لم تجد Realtime Database في القائمة:
- قد تكون محذوفة
- أنشئ قاعدة بيانات جديدة
- انسخ الـ `databaseURL` الجديد إلى `firebase-config.js`
