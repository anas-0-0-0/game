# 🚀 دليل النشر على Netlify

## الطريقة الأولى: السحب والإفلات (الأسهل)

1. اذهب إلى [netlify.com](https://www.netlify.com)
2. سجل دخول أو أنشئ حساب جديد
3. في لوحة التحكم، اسحب مجلد المشروع بالكامل إلى منطقة "Deploy"
4. انتظر حتى يكتمل النشر
5. احصل على الرابط الخاص بك!

## الطريقة الثانية: عبر Git (موصى بها)

### إذا كان لديك حساب GitHub/GitLab/Bitbucket:

1. ارفع المشروع إلى مستودع Git:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin [رابط المستودع]
   git push -u origin main
   ```

2. في Netlify:
   - اضغط "New site from Git"
   - اختر منصة Git الخاصة بك
   - اختر المستودع
   - Build command: اتركه فارغاً
   - Publish directory: `.` (نقطة)
   - اضغط "Deploy site"

## إعدادات Netlify الموصى بها

- **Build command:** (فارغ - لا حاجة لبناء)
- **Publish directory:** `.` (نقطة)
- **Node version:** (غير مطلوب)

## بعد النشر

- ستحصل على رابط مثل: `your-site.netlify.app`
- يمكنك تغيير اسم الموقع من Settings > Site details > Change site name
- يمكنك إضافة نطاق مخصص من Domain settings

## ملاحظات مهمة

✅ جميع الملفات جاهزة للنشر  
✅ التصميم متجاوب مع الجوال والتابلت  
✅ لا حاجة لإعدادات إضافية  

## الدعم

إذا واجهت أي مشكلة، تأكد من:
- رفع جميع الملفات (.html, .js, .css)
- أن `styles-common.css` موجود في نفس المجلد
- أن `questions-data.js` موجود
